# utils

Служебные утилиты.

Содержимое:
- `logging_setup.py` — конфигурация корневого логирования (JSON в файл и консоль, ротация).

Использование:
- `src/app.py` вызывает настройку логирования при старте.
